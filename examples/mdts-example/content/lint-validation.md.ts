import { md } from '@mdts/cli'

export const meta = {
  title: 'Lint validation',
}

export default md`
At the end of the day, it is important to note that this intentionally overlong sentence exists only to demonstrate that mdts lint reports both prose findings and Markdown style violations from generated output.
`
